# 作成済みファイル確認
ls -la complete_findings.*

# CSVファイルが存在するかチェック
if [ -f "complete_findings.csv" ]; then
    echo "✅ CSVファイル作成済み"
    echo "サイズ: $(ls -lh complete_findings.csv | awk '{print $5}')"
    echo "行数: $(wc -l < complete_findings.csv)"
else
    echo "⏳ CSVファイルはまだ作成中です"
fi
