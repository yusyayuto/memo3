# より強力な削除
echo "=== 強制削除実行 ==="

# 隠しファイルも含めて削除
rm -rf *.csv *.json *.log *.out *.sh findings_* temp_* all_findings* complete_findings* simple_findings* rescued_*

# nohup関連も削除
rm -f nohup.out

# その他一時ファイル
rm -f progress.log error.log

echo "✅ 強制削除完了"
ls -la
