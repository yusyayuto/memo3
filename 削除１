# 本当に容量が厳しい場合のみ
echo "=== 完全クリーンアップ（最終手段） ==="
echo "⚠️  すべてのCSV・JSON・ログファイルを削除します"
read -p "本当にすべて削除しますか？ (y/N): " -n 1 -r
echo

if [[ $REPLY =~ ^[Yy]$ ]]; then
    rm -f *.csv *.json *.log *.out findings_*
    echo "✅ 完全クリーンアップ完了"
else
    echo "完全クリーンアップをキャンセル"
fi
